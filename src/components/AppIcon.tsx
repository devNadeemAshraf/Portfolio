const AppIcon = () => {
  return (
    <span className="text-lg font-bold text-primary cursor-pointer lowercase hover_underline">
      nadeem
      <span className="font-normal text-muted-foreground">ashraf.dev</span>
    </span>
  );
};

export default AppIcon;
